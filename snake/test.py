from LinearQNet import Linear_QNet

my_model = Linear_QNet(40,
                       30, 2, 
                       30, 0,
                       30, 0,
                       3, 
                       100)
print(my_model)
